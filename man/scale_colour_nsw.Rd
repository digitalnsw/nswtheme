% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_nsw.R
\name{scale_colour_nsw}
\alias{scale_colour_nsw}
\alias{scale_fill_nsw}
\alias{scale_color_nsw}
\title{NSW colour and fill scales}
\usage{
scale_colour_nsw(
  ...,
  palette = pal_waratah("qual"),
  aesthetics = "colour",
  na.value = "grey50"
)

scale_fill_nsw(
  ...,
  palette = pal_waratah("qual"),
  aesthetics = "fill",
  na.value = "grey50"
)

scale_color_nsw(
  ...,
  palette = pal_waratah("qual"),
  aesthetics = "colour",
  na.value = "grey50"
)
}
\arguments{
\item{...}{passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}} or
\code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}}.}

\item{palette}{a palette object from \code{\link[=pal_nsw]{pal_nsw()}} or \code{\link[=pal_waratah]{pal_waratah()}}, or a
vector of colours named as in \code{\link{nsw_colours}}.}

\item{aesthetics}{aesthetics this scale applies to.}

\item{na.value}{colour used for missing values.}
}
\value{
A ggplot2 scale, to add to a plot.
}
\description{
Applies an NSW palette to the \code{colour} or \code{fill} aesthetic. The palette
decides the kind of scale: a discrete palette gives a discrete scale, a
continuous one a continuous scale.
}
\details{
From ggplot2 4.0.0 \code{\link[=theme_nsw]{theme_nsw()}} sets default palettes itself, so these
scales are only needed when you want to change the defaults.
With earlier ggplot2 versions, you need these scales also for defaults.
}
\examples{
library(ggplot2)

ggplot(palmerpenguins::penguins) +
  geom_point(aes(bill_length_mm, flipper_length_mm, colour = species)) +
  scale_colour_nsw()

ggplot(palmerpenguins::penguins) +
  geom_point(aes(bill_length_mm, flipper_length_mm, colour = body_mass_g)) +
  scale_colour_nsw(palette = pal_waratah("seq"))

ggplot(palmerpenguins::penguins) +
  geom_bar(aes(island, fill = species)) +
  scale_fill_nsw(palette = pal_nsw_manual(c("blue_01", "red_02", "teal_02")))

}
\seealso{
\code{\link[=theme_nsw]{theme_nsw()}}

Other palettes:
\code{\link[=pal_nsw]{pal_nsw()}},
\code{\link[=pal_waratah]{pal_waratah()}}
}
\concept{palettes}
